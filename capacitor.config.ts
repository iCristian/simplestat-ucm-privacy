import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cristiancarreno.simplestat_ucm',
  appName: 'simpleStat',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
